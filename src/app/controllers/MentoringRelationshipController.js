const Yup = require('yup');

const connection = require('../../database/connection');
const Logger = require('../../lib/logger');

class Mentoring {
  async create(req, res) {
    Logger.header('controller - mentoring relationship - create');

    const { }
  }
}

module.exports = new Mentoring();